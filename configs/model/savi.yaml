name: "savi"
type: "savi"

resolution: [64, 64]
n_sample_frames: 6

# Paper-aligned defaults: the SAVi wrapper provides correct defaults
# (num_iterations=3, enc_channels=[3,64,64,64,64], pred_type='transformer',
#  pred_rnn=True, kld_method='none'). Only override what differs.

slot_dict:
  num_slots: 4
  slot_size: 64

loss_dict:
  kld_method: "none"

weight_dict:
  recon: 1.0
  mask: 1.0
